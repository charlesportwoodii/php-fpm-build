#!/bin/bash

set +e

which systemctl
if [ "$?" == 1 ]
then
    $(which service) php-fpm stop
    rm -rf /etc/init.d/php-fpm
else
    $(which systemctl) stop php-fpm.service
    $(which systemctl) disable php-fpm.service
    rm -rf /lib/systemd/system/php-fpm.service
fi

exit 0
